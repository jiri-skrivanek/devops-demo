name: First Workflow

on: push

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Hello, GitHub Actions!"
      - name: Print second message
        run: echo "02 - Hello, GitHub Actions!"
  helloWorld:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Hello World!"
        
